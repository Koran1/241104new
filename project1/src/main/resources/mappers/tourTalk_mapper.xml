<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tourTalk">
	<insert id="insert" parameterType="tourtvo">
		insert into tourTalk(userId, tourTalkContent, tourTalkReg, f_name, hit, active, old_f_name) 
		values(#{userId},#{tourTalkContent},now(),#{f_name},0,0,#{old_f_name})
	</insert>

</mapper>